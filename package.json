{
  "private": true,
  "packageManager": "pnpm@9.0.6",
  "name": "blue-challenge",
  "scripts": {
    "dev": "turbo run dev --parallel --",
    "build": "turbo run build --",
    "web": "pnpm --filter \"@blue-challenge/web\" --",
    "api": "pnpm --filter \"@blue-challenge/api\" --"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "vitest": "^1.6.0",
    "@types/node": "^20.14.9",
    "typescript": "^5.5.3",
    "turbo": "^2.0.6"
  }
}
